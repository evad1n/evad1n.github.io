branches:
  only:
  - main

language: node_js
node_js: node

cache:
  directories:
    - 'node_modules'

install:
  - yarn
  - yarn generate

script:
  - echo "Skipping tests"

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN 
  target-branch: gh-pages
  local-dir: dist
  on:
    branch: main
